# Settings configured earlier take precedence over settings configured later.
Include 1Password/config
Include conf.d/*

# Defaults.
Host *
  CanonicalizeHostname  yes
  CanonicalizeMaxDots   0
  ConnectTimeout        10
  ControlMaster         auto
  ControlPath           "~/.ssh/s/%C"
  ControlPersist        5
  EscapeChar            `
  IdentityAgent         "~/.ssh/agent.sock"
  ServerAliveInterval   60
